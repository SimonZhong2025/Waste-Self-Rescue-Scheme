# 构造函数

+ ```cpp
  class Log
  {
  public:
  	Log() = delete;   
  }
  ```

  这样可以让默认构造函数不存在。因此不能实例化这个类的对象。

  ```cpp
  class Log
  {
  private:
  	Log() {};   
  }
  ```

  这样也能达到同样的效果，让构造函数为 `private` 。

+ 